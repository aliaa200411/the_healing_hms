<odoo>
    <!-- Action لفتح السجل الطبي -->
    <record id="action_hospital_patient" model="ir.actions.act_window">
        <field name="name">Medical Record</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">list,form</field> <!-- list بدل tree -->
    </record>

    <!-- Action الأطباء -->
    <record id="action_doctor" model="ir.actions.act_window">
        <field name="name">Doctors</field>
        <field name="res_model">hospital.doctor</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- قائمة الأطباء -->
    <record id="view_doctor_list" model="ir.ui.view">
        <field name="name">hospital.doctor.list</field>
        <field name="model">hospital.doctor</field>
        <field name="arch" type="xml">
            <list>
                <field name="doctor_id"/>
                <field name="user_id"/>
                <field name="specialization_id"/>
                <field name="department_id"/>
                <field name="experience_years"/>
                <field name="working_hours"/>
                <field name="is_available"/>
            </list>
        </field>
    </record>

    <!-- نموذج الطبيب -->
    <record id="view_doctor_form" model="ir.ui.view">
        <field name="name">hospital.doctor.form</field>
        <field name="model">hospital.doctor</field>
        <field name="arch" type="xml">
            <form string="Doctor">
                <sheet>
                    <group>
                        <field name="doctor_id"/>
                        <field name="user_id"/>
                        <field name="specialization_id"/>
                        <field name="department_id"/>
                        <field name="experience_years"/>
                        <field name="working_hours"/>
                        <field name="is_available"/>
                        <field name="hire_date"/>
                        <field name="salary"/>
                    </group>

                    <notebook>
                        <page string="Patients">
                            <field name="patient_ids">
                                <list editable="bottom">
                                    <field name="first_name"/>
                                    <field name="last_name"/>
                                    <field name="dob"/>
                                    <field name="gender"/>
                                    <field name="diagnosis"/>
                                    <button name="%(action_hospital_patient)d" type="action" string="Medical Record" class="btn-primary"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
